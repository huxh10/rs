enclave {
    trusted {
        public uint32_t ecall_memory_limitation(uint32_t size);
        public uint32_t ecall_edger8r_limitation([in, size = msg_size] void *msg, size_t msg_size);
    };

    untrusted {
        void ocall_print_string([in, string] const char *str);
    };
};
